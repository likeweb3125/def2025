# Node.js 20 LTS 버전 사용
FROM node:20-alpine

# 작업 디렉토리 설정
WORKDIR /app

# package.json과 package-lock.json 복사
COPY package*.json ./

# 모든 의존성 설치 (개발 의존성 포함)
RUN npm ci

# 포트 3000 노출
EXPOSE 3000

# 개발 서버 실행 (볼륨 마운트로 소스 코드는 실시간 반영)
CMD ["npm", "run", "dev"]
